# Cargo.toml
[package]
name = "mini_html_editor"
version = "0.1.0"
edition = "2021"

[dependencies]
wry = "0.35.2"
tao = "=0.25.0" # استفاده از علامت مساوی گیت‌هاب را مجبور به استفاده از نسخه همگام می‌کند
